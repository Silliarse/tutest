version: 2
jobs:
  build:
    docker:
      - image: circleci/python:3.7.0
    steps:
      - checkout
      - run: echo "A first hello"
      - run: python test.py
  test:
    docker:
      - image: sonarqube
    steps:
      - checkout
      - run: echo "code coverage"
      - run: sudo chmod a+rw /
      - run: sonar-scanner \
         -Dsonar.projectKey=Silliarse_tutest \
         -Dsonar.organization=silliarse-github \
         -Dsonar.sources=. \
         -Dsonar.host.url=https://sonarcloud.io \
         -Dsonar.login=f0097e357d891a0a229766649346c4d2a3e470df

workflows:
    version: 2
    build_and_test:
      jobs:
       - build
       - test
